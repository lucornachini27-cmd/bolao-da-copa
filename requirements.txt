# --- API / Web ---
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9      # formulários OAuth2 (login)

# --- Banco de dados / ORM / Migrations ---
sqlalchemy>=2.0
alembic>=1.13
psycopg2-binary>=2.9         # driver PostgreSQL (produção)

# --- Validação / Config ---
pydantic>=2.6
pydantic-settings>=2.2
email-validator>=2.1         # EmailStr

# --- Segurança (auth) ---
PyJWT>=2.8
bcrypt>=4.1

# --- Consumo da API externa (ESPN scoreboard) ---
httpx>=0.27

# --- Agendador (sincronização automática em segundo plano) ---
apscheduler>=3.10

# --- Fuso horário (Windows não traz a base de tz; tzdata fornece) ---
tzdata>=2024.1

# --- Testes ---
pytest>=8.0

# --- Automação do WhatsApp / Screenshots ---
playwright>=1.40
